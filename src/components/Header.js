import React from 'react';

const Header = () => {

    return (
        <div className="ui secondary pointing menu">
            <h2 className="headingBody active item">Dictioned!</h2>
        </div>
    );
};

export default Header;