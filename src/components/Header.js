import React from 'react';
import "../style/Header.css";

const Header = () => {

    return (
        <div className="ui secondary pointing menu HeaderContent">
            <h2 className="headingBody active item">Dictioned!</h2>
        </div>
    );
};

export default Header;